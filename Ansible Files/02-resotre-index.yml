---
- name: Deploy configuration file
  hosts: webservers
  become: yes
  tasks:
    - name: Copy config file
      copy:
        src: index.html
        dest: /var/www/html/index.html
      notify: Restart nginx
  handlers:
    - name: Restart nginx
      service:
        name: nginx
        state: restarted
